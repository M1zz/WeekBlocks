//
//  TodoAccess.swift
//  WeekBlocks
//
//  **이 기기에서 적은 것이 다른 기기로 건너가는가 — 언제나 그렇다.**
//
//  한때 이 자리에 값을 걸었다. 처음엔 '적기'에, 다음엔 '아이폰으로 건너가기'에.
//  둘 다 틀렸다. 적기는 이 앱의 본체라 잠그면 앱이 잠기고, 건너가기는 같은 iCloud를
//  쓰는 사람에게 **원래 되던 것**이라 막으면 뺏는 것으로 읽힌다(1.1.2 릴리즈 노트의
//  "이 맥에서 적은 것은 아이폰에 가지 않습니다"가 그 문장이었다).
//
//  값을 받는 것은 이제 **쌓여야 보이는 것**뿐이다 (→ WeekBlocksSpec.monetization, MacEntitlement).
//
//  ⚠️ 이 값을 쓰는 커튼 규칙(→ TodoSharing.swift)은 iOS '욕망의 무지개'와 짝이라 그대로 둔다.
//     여기가 참이면 새 줄은 열린 채로 찍히고, 켤 때 한 번 닫혀 있던 내 줄도 도로 열린다.
//

import Foundation

enum TodoAccess {

    /// 이 맥에서 적은 것이 아이폰으로 건너가는가. **다시 거짓으로 만들지 말 것.**
    static var canSync: Bool { true }
}
